apiVersion: v1
kind: Service
metadata:
  labels:

    app.kubernetes.io/name: tecradar-dev-backend
  name: tecradar-dev-backend
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 31685
  selector:
    app.kubernetes.io/name: tecradar-dev-backend
  type: NodePort
