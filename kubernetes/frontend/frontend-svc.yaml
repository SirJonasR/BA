apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: tecradar-frontend
  name: tecradar-frontend
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 31745
  selector:
    app.kubernetes.io/name: tecradar-frontend
  type: NodePort
